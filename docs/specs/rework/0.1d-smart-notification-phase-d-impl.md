---
entity_id: smart_notification_engine_phase_d
type: module
created: 2026-03-22
updated: 2026-03-22
status: draft
version: "1.0"
tags: [notifications, rework, phase-d, review, nudge, settings]
---

# RW_0.1d Phase D — Smart Notification Engine (Review/Nudge + Settings UI)

## Approval

- [ ] Approved

## Purpose

Fuellt die leeren Stub-Methoden `buildReviewRequests()` und `buildNudgeRequests()` mit echtem Inhalt (Abend-Review, Morning-Nudge, Arbeitszeit-Nudges) und ergaenzt einen Notification-Profil-Picker in der Settings UI beider Plattformen. Profil-Wechsel triggert Reconcile.

## Source

- **File:** `Sources/Services/SmartNotificationEngine.swift` (MODIFY)
- **Identifier:** `enum SmartNotificationEngine` — Methoden `buildReviewRequests()` und `buildNudgeRequests()`
- **Weitere Files:**
  - `Sources/Views/SettingsView.swift` (MODIFY)
  - `FocusBloxMac/MacSettingsView.swift` (MODIFY)

## Dependencies

| Entity | Type | Purpose |
|--------|------|---------|
| `SmartNotificationEngine` (Phase A-C) | service | Basis-Engine mit reconcile(), Budget-Konstanten, Profil-Logik |
| `AppSettings` | model | `notificationProfile` Property existiert bereits (Z56-67) |
| `NotificationService` | service | Referenz-Pattern fuer `build*Request`-Methoden (wird NICHT modifiziert) |
| `UNUserNotificationCenter` | system | Notification-Requests werden von reconcile() eingeplant |
| `SettingsView` | view | iOS Settings — bekommt Profil-Picker |
| `MacSettingsView` | view | macOS Settings — bekommt Profil-Picker im Notifications-Tab |

## Implementation Details

### 1. `Sources/Services/SmartNotificationEngine.swift` (~+70 LoC, MODIFY)

#### 1a. `buildReviewRequests()` — Signatur aendern + Inhalt

```swift
// VORHER (Z309-313):
// MARK: - Prio 3: Review / Morning Requests (Phase C)

private static func buildReviewRequests() -> [UNNotificationRequest] {
    return []
}

// NACHHER:
// MARK: - Prio 3: Review / Morning Requests

static func buildReviewRequests(now: Date = Date()) -> [UNNotificationRequest] {
    let cal = Calendar.current
    let today = cal.startOfDay(for: now)
    var requests: [UNNotificationRequest] = []

    // 1. Evening Review — 20:00 heute
    let eveningHour = 20
    if let eveningDate = cal.date(bySettingHour: eveningHour, minute: 0, second: 0, of: today),
       eveningDate > now {
        let content = UNMutableNotificationContent()
        content.title = "Tagesreview"
        content.body = "Zeit fuer dein Tagesreview — was hast du heute geschafft?"
        content.sound = .default

        let interval = eveningDate.timeIntervalSince(now)
        let trigger = UNTimeIntervalNotificationTrigger(timeInterval: interval, repeats: false)
        let dateStr = Self.dateString(from: today)
        requests.append(UNNotificationRequest(
            identifier: "focusblox.review.\(dateStr)",
            content: content,
            trigger: trigger
        ))
    }

    // 2. Morning Nudge — 08:00 morgen
    if let tomorrow = cal.date(byAdding: .day, value: 1, to: today),
       let morningDate = cal.date(bySettingHour: 8, minute: 0, second: 0, of: tomorrow),
       morningDate > now {
        let content = UNMutableNotificationContent()
        content.title = "Guten Morgen"
        content.body = "Dein Tag wartet — was packst du heute an?"
        content.sound = .default

        let interval = morningDate.timeIntervalSince(now)
        let trigger = UNTimeIntervalNotificationTrigger(timeInterval: interval, repeats: false)
        let dateStr = Self.dateString(from: tomorrow)
        requests.append(UNNotificationRequest(
            identifier: "focusblox.morning.\(dateStr)",
            content: content,
            trigger: trigger
        ))
    }

    return Array(requests.prefix(budgetReview))
}
```

#### 1b. `buildNudgeRequests()` — Signatur aendern + Inhalt

```swift
// VORHER (Z315-319):
// MARK: - Prio 4: Nudge Requests (Phase C)

private static func buildNudgeRequests() -> [UNNotificationRequest] {
    return []
}

// NACHHER:
// MARK: - Prio 4: Nudge Requests

static func buildNudgeRequests(now: Date = Date()) -> [UNNotificationRequest] {
    let cal = Calendar.current
    let today = cal.startOfDay(for: now)

    // Feste Arbeitszeit-Slots: 9, 11, 13, 15, 17, 19 Uhr
    let nudgeHours = [9, 11, 13, 15, 17, 19]

    // Rotierende, veralterungssichere Texte
    let nudgeTexts: [(title: String, body: String)] = [
        ("Wie laeuft dein Tag?", "Schau mal in dein Backlog — vielleicht ist ein Quick Win dabei."),
        ("Zeit fuer den naechsten Sprint?", "Ein kurzer Focus Block kann viel bewegen."),
        ("Dein Backlog wartet", "Welchen Task koenntest du jetzt angehen?"),
        ("Kurze Pause vorbei?", "Der naechste kleine Schritt wartet auf dich."),
        ("Halbzeit!", "Guter Zeitpunkt fuer einen Focus Sprint."),
        ("Endspurt!", "Noch ein Task vor Feierabend?"),
    ]

    var requests: [UNNotificationRequest] = []

    for (index, hour) in nudgeHours.enumerated() {
        guard let fireDate = cal.date(bySettingHour: hour, minute: 0, second: 0, of: today),
              fireDate > now else { continue }

        let text = nudgeTexts[index % nudgeTexts.count]
        let content = UNMutableNotificationContent()
        content.title = text.title
        content.body = text.body
        content.sound = .default

        let interval = fireDate.timeIntervalSince(now)
        let trigger = UNTimeIntervalNotificationTrigger(timeInterval: interval, repeats: false)
        requests.append(UNNotificationRequest(
            identifier: "focusblox.nudge.work.\(hour)",
            content: content,
            trigger: trigger
        ))
    }

    return Array(requests.prefix(budgetNudges))
}
```

#### 1c. Helper-Methode fuer Date-String

```swift
// MARK: - Helpers

private static func dateString(from date: Date) -> String {
    let formatter = DateFormatter()
    formatter.dateFormat = "yyyy-MM-dd"
    return formatter.string(from: date)
}
```

#### 1d. Aufruforte in `buildAllRequests` aktualisieren (beide Overloads)

```swift
// VORHER (in beiden buildAllRequests-Methoden):
requests += buildReviewRequests()
// ...
requests += buildNudgeRequests()

// NACHHER:
requests += buildReviewRequests(now: Date())
// ...
requests += buildNudgeRequests(now: Date())
```

Betrifft 4 Zeilen (2 pro Overload).

---

### 2. `Sources/Views/SettingsView.swift` (~+30 LoC, MODIFY)

#### 2a. Neue @AppStorage Property

```swift
// Nach den bestehenden @AppStorage-Deklarationen (Z20):
@AppStorage("notificationProfile") private var notificationProfileRaw: String = "balanced"
```

#### 2b. Neue Section fuer Profil-Picker (vor "Benachrichtigungen"-Section, Z35)

```swift
// Section: Notification Profile
Section {
    Picker("Benachrichtigungsprofil", selection: $notificationProfileRaw) {
        Text("Leise").tag("quiet")
        Text("Ausgeglichen").tag("balanced")
        Text("Aktiv").tag("active")
    }
    .accessibilityIdentifier("notificationProfilePicker")
} header: {
    Text("Profil")
} footer: {
    Text("Leise: nur Sprint-Timer. Ausgeglichen: Timer + Fristen + Tagesreview. Aktiv: alle inkl. Motivations-Nudges.")
}
```

#### 2c. onChange-Handler fuer Profil-Wechsel (auf der Form)

```swift
.onChange(of: notificationProfileRaw) { _, _ in
    Task {
        await SmartNotificationEngine.reconcile(
            reason: .profileChanged,
            context: modelContext,
            eventKitRepo: eventKitRepo
        )
    }
}
```

---

### 3. `FocusBloxMac/MacSettingsView.swift` (~+30 LoC, MODIFY)

#### 3a. Neue @AppStorage Property

```swift
// Nach den bestehenden @AppStorage-Deklarationen (Z28):
@AppStorage("notificationProfile") private var notificationProfileRaw: String = "balanced"
```

#### 3b. Profil-Picker im Notifications-Tab (am Anfang von `notificationsTab`, Z260)

```swift
Section {
    Picker("Benachrichtigungsprofil", selection: $notificationProfileRaw) {
        Text("Leise").tag("quiet")
        Text("Ausgeglichen").tag("balanced")
        Text("Aktiv").tag("active")
    }
    .accessibilityIdentifier("notificationProfilePicker")
} header: {
    Text("Profil")
} footer: {
    Text("Leise: nur Sprint-Timer. Ausgeglichen: Timer + Fristen + Tagesreview. Aktiv: alle inkl. Motivations-Nudges.")
}
```

#### 3c. onChange-Handler (auf dem Form des Notifications-Tab)

```swift
.onChange(of: notificationProfileRaw) { _, _ in
    Task {
        await SmartNotificationEngine.reconcile(
            reason: .profileChanged,
            context: modelContext,
            eventKitRepo: eventKitRepo
        )
    }
}
```

---

## Expected Behavior

- **Input:** `reconcile()` wird aufgerufen (alle bestehenden Trigger + neuer Profil-Wechsel-Trigger)
- **Output:**
  - Profil "quiet": 0 Review, 0 Nudge Requests (unveraendert)
  - Profil "balanced": bis zu 2 Review Requests (Evening 20:00 + Morning 08:00 morgen)
  - Profil "active": bis zu 2 Review + bis zu 6 Nudge Requests (je nach Uhrzeit)
  - Gesamt immer <= 64 Requests
- **Side effects:**
  - `buildReviewRequests()` und `buildNudgeRequests()` sind nicht mehr leer
  - Beide Methoden sind `internal static` statt `private static` (fuer Testbarkeit)
  - Settings UI zeigt Profil-Picker auf beiden Plattformen
  - Profil-Wechsel triggert sofortigen Full-Reconcile

## Identifier-Konvention

| Kategorie | ID-Pattern | Beispiel |
|-----------|-----------|---------|
| Evening Review | `focusblox.review.{YYYY-MM-dd}` | `focusblox.review.2026-03-22` |
| Morning Nudge | `focusblox.morning.{YYYY-MM-dd}` | `focusblox.morning.2026-03-23` |
| Work Nudges | `focusblox.nudge.work.{HH}` | `focusblox.nudge.work.13` |

## Notification-Texte

| Typ | Title | Body |
|-----|-------|------|
| Evening Review | "Tagesreview" | "Zeit fuer dein Tagesreview — was hast du heute geschafft?" |
| Morning | "Guten Morgen" | "Dein Tag wartet — was packst du heute an?" |
| Nudge 09:00 | "Wie laeuft dein Tag?" | "Schau mal in dein Backlog — vielleicht ist ein Quick Win dabei." |
| Nudge 11:00 | "Zeit fuer den naechsten Sprint?" | "Ein kurzer Focus Block kann viel bewegen." |
| Nudge 13:00 | "Dein Backlog wartet" | "Welchen Task koenntest du jetzt angehen?" |
| Nudge 15:00 | "Kurze Pause vorbei?" | "Der naechste kleine Schritt wartet auf dich." |
| Nudge 17:00 | "Halbzeit!" | "Guter Zeitpunkt fuer einen Focus Sprint." |
| Nudge 19:00 | "Endspurt!" | "Noch ein Task vor Feierabend?" |

## Tests (TDD RED Phase)

### Unit Tests: `FocusBloxTests/SmartNotificationEnginePhaseDTests.swift`

#### buildReviewRequests Tests

```swift
/// Verhalten: Um 15:00 liefert buildReviewRequests genau 2 Requests (Evening + Morning).
/// Bricht wenn: Methode weiterhin leeres Array zurueckgibt oder falsche IDs generiert.
func test_buildReviewRequests_afternoon_returns2Requests()

/// Verhalten: Um 21:00 liefert buildReviewRequests genau 1 Request (nur Morning, Evening ist vorbei).
/// Bricht wenn: Vergangene Evening-Time trotzdem eingeplant wird.
func test_buildReviewRequests_afterEvening_returns1Request()

/// Verhalten: Review-IDs folgen Schema focusblox.review.{date} und focusblox.morning.{date}.
/// Bricht wenn: Identifier-Schema geaendert wird.
func test_buildReviewRequests_identifierSchema()

/// Verhalten: Nie mehr als budgetReview (2) Requests.
/// Bricht wenn: Budget-Cap entfernt oder erhoeht wird.
func test_buildReviewRequests_neverExceedsBudget()
```

#### buildNudgeRequests Tests

```swift
/// Verhalten: Um 08:00 liefert buildNudgeRequests 6 Requests (alle Arbeitszeit-Slots ab 09:00).
/// Bricht wenn: Methode weiterhin leeres Array zurueckgibt oder Slots fehlen.
func test_buildNudgeRequests_morning_returns6Requests()

/// Verhalten: Um 14:00 liefert buildNudgeRequests 3 Requests (15, 17, 19 Uhr).
/// Bricht wenn: Vergangene Slots trotzdem eingeplant werden.
func test_buildNudgeRequests_afternoon_returnsOnlyFutureSlots()

/// Verhalten: Um 20:00 liefert buildNudgeRequests 0 Requests (alle Slots vorbei).
/// Bricht wenn: Slots ausserhalb der Arbeitszeit generiert werden.
func test_buildNudgeRequests_evening_returns0()

/// Verhalten: Nudge-IDs folgen Schema focusblox.nudge.work.{HH}.
/// Bricht wenn: Identifier-Schema geaendert wird.
func test_buildNudgeRequests_identifierSchema()

/// Verhalten: Nie mehr als budgetNudges (10) Requests.
/// Bricht wenn: Budget-Cap entfernt wird.
func test_buildNudgeRequests_neverExceedsBudget()
```

#### Budget-Integration Tests

```swift
/// Verhalten: Bei Profil "balanced" enthält buildAllRequests Review-Requests aber keine Nudges.
/// Bricht wenn: Profil-Gating fuer Review oder Nudge falsch ist.
func test_buildAllRequests_balanced_includesReview_excludesNudges()

/// Verhalten: Bei Profil "active" enthält buildAllRequests sowohl Review als auch Nudge-Requests.
/// Bricht wenn: Active-Profil Nudges nicht einschliesst.
func test_buildAllRequests_active_includesReviewAndNudges()

/// Verhalten: Bei Profil "quiet" enthält buildAllRequests weder Review noch Nudges.
/// Bricht wenn: Quiet-Profil ungewollt Review/Nudges einschliesst.
func test_buildAllRequests_quiet_excludesReviewAndNudges()

/// Verhalten: Gesamt-Budget bleibt <= 64 auch mit Review + Nudge-Requests.
/// Bricht wenn: Budget-Cap durch neue Requests ueberschritten wird.
func test_totalBudget_withReviewAndNudges_neverExceeds64()
```

#### Regression Tests

```swift
/// Verhalten: Phase A/B/C Container-Overload funktioniert weiterhin.
/// Bricht wenn: Signatur-Aenderungen bestehende Overloads brechen.
func test_containerOverload_stillFunctional()
```

### UI Tests: `FocusBloxUITests/SmartNotificationPhaseDUITests.swift`

```swift
/// Verhalten: Settings zeigt Profil-Picker mit Identifier "notificationProfilePicker".
/// Bricht wenn: Picker nicht existiert oder falschen Identifier hat.
func test_settingsShowsProfilePicker()

/// Verhalten: Profil-Picker hat 3 Optionen (Leise, Ausgeglichen, Aktiv).
/// Bricht wenn: Eine Option fehlt oder falsch benannt ist.
func test_profilePickerHasThreeOptions()
```

## Known Limitations

- Review-Zeiten sind hardcoded (20:00 + 08:00) — konfigurierbar in spaeterer Phase
- Nudge-Zeiten sind feste Intervalle (9-19 Uhr alle 2h) — keine Activity-Gap-Detection
- Nudge-Texte sind statisch pro Slot — keine Personalisierung basierend auf BehavioralProfile (kommt mit RW_0.2)
- Nudges werden nur fuer den aktuellen Tag geplant — bei Reconcile am naechsten Tag werden neue generiert
- `NotificationService` wird in Phase D NICHT modifiziert — neue build-Logik lebt direkt in der Engine

## Changelog

- 2026-03-22: Initial spec created (Phase D — Review/Nudge + Settings UI)
  - Scope: 3 Dateien Production, ~130 LoC delta
  - Naechste Phase: RW_0.2 (BehavioralProfileService — Nudge-Personalisierung)

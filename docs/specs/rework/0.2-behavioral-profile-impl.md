---
entity_id: behavioral_profile_service_0_2
type: module
created: 2026-03-23
updated: 2026-03-23
status: draft
version: "1.0"
tags: [behavioral-profile, rework, statistics, services]
---

# RW_0.2 — BehavioralProfileService (Phase 1: Core Statistics)

## Approval

- [ ] Approved

## Purpose

Deterministischer Service, der aus bestehenden Task- und FocusBlock-Daten ein Verhaltensprofil berechnet. Rolling Window: 28 Tage. Kein LLM, keine AI — reine Statistik. Liefert drei Kernmetriken: Tageszeit-Affinitaet pro Kategorie, taegliche Kapazitaet und Schaetz-Genauigkeit. Konsumierende Services (NextUpSuggestionService, EmotionalNudgeService) lesen das gecachte Profil.

## Source

- **File:** `Sources/Models/BehavioralProfile.swift` (CREATE)
- **Identifier:** `struct BehavioralProfile`, `enum DayPeriod`
- **Weitere Files:**
  - `Sources/Services/BehavioralProfileService.swift` (CREATE)
  - `FocusBloxTests/BehavioralProfileServiceTests.swift` (CREATE)

## Dependencies

| Entity | Type | Purpose |
|--------|------|---------|
| `LocalTask` | model | Datenquelle: `completedAt`, `taskType`, `estimatedDuration`, `isCompleted` |
| `FocusBlock` | model | Datenquelle: `taskTimes: [String: Int]` (TaskID -> Sekunden tatsaechlich gearbeitet) |
| `TaskCategory` | enum | 5 Werte: income, maintenance, recharge, learning, giving_back |
| `CategoryStatsService` | service | Referenz-Pattern fuer `enum`-Architektur mit static methods |

## Implementation Details

### 1. `Sources/Models/BehavioralProfile.swift` (~45 LoC, CREATE)

```swift
import Foundation

/// Tageszeit-Fenster fuer Affinitaets-Berechnung.
/// morning: 06-12, afternoon: 12-18, evening: 18-06 (uebernaechtigend)
enum DayPeriod: String, CaseIterable, Hashable {
    case morning   = "morning"    // 06:00–11:59
    case afternoon = "afternoon"  // 12:00–17:59
    case evening   = "evening"    // 18:00–05:59
}

/// Berechnetes Verhaltensprofil aus den letzten 28 Tagen.
/// Wird von BehavioralProfileService erzeugt und in-memory gecacht.
/// Alle Felder koennen nil sein wenn nicht genuegend Daten vorliegen.
struct BehavioralProfile {
    /// Zeitpunkt der Berechnung (fuer Cache-Validierung)
    let computedAt: Date

    /// Tageszeit-Affinitaet pro Kategorie.
    /// [Kategorie: [Tageszeit: Anteil 0.0-1.0]]
    /// nil = unter Mindestschwelle (< 10 abgeschlossene Tasks mit Kategorie)
    let categoryTimeAffinity: [TaskCategory: [DayPeriod: Double]]?

    /// Durchschnittliche Anzahl erledigter Tasks pro aktivem Tag.
    /// nil = unter Mindestschwelle (< 5 aktive Tage im Fenster)
    let avgTasksPerDay: Double?

    /// Durchschnittliche tatsaechliche Arbeitszeit pro aktivem Tag (Minuten).
    /// Basiert auf FocusBlock.taskTimes-Summen.
    /// nil = unter Mindestschwelle (< 5 aktive Tage im Fenster)
    let avgMinutesPerDay: Double?

    /// Schaetz-Genauigkeits-Faktor.
    /// 1.0 = perfekt, 1.5 = User braucht 50% laenger als geschaetzt.
    /// nil = unter Mindestschwelle (< 10 Tasks mit beiden Werten)
    let estimationFactor: Double?
}
```

---

### 2. `Sources/Services/BehavioralProfileService.swift` (~130 LoC, CREATE)

```swift
import Foundation

/// Berechnet ein Verhaltensprofil aus LocalTask- und FocusBlock-Daten.
/// Pattern: enum mit static methods (wie CategoryStatsService).
/// Cache: in-memory, kein AppSettings.
enum BehavioralProfileService {

    // MARK: - Cache

    /// In-memory Cache. Wird invalidiert wenn das gecachte Profil
    /// von einem anderen Kalendertag stammt als heute.
    private static var _cache: (profile: BehavioralProfile, date: Date)?

    // MARK: - Minimum-Sample-Schwellen

    /// Mindestanzahl Tasks mit gesetzter Kategorie fuer Affinitaets-Berechnung
    static let minTasksForAffinity: Int = 10

    /// Mindestanzahl aktiver Tage fuer Kapazitaets-Berechnung
    static let minActiveDaysForCapacity: Int = 5

    /// Mindestanzahl Tasks mit beiden Dauer-Werten fuer Schaetz-Genauigkeit
    static let minTasksForEstimation: Int = 10

    // MARK: - Public API

    /// Gibt das gecachte Profil zurueck oder berechnet es neu.
    /// Cache wird invalidiert wenn computedAt von einem anderen Kalendertag ist.
    /// - Parameters:
    ///   - tasks: Alle LocalTasks (Service filtert intern auf completed + 28-Tage-Fenster)
    ///   - focusBlocks: FocusBlocks im 28-Tage-Fenster (fuer taskTimes-Daten)
    ///   - now: Referenzzeitpunkt (default: Date(), injizierbar fuer Tests)
    /// - Returns: BehavioralProfile mit berechneten Metriken (nil-Felder wenn zu wenig Daten)
    static func profile(
        tasks: [LocalTask],
        focusBlocks: [FocusBlock],
        now: Date = Date()
    ) -> BehavioralProfile {
        let calendar = Calendar.current
        let today = calendar.startOfDay(for: now)

        // Cache-Hit pruefen: gleicher Kalendertag?
        if let cached = _cache,
           calendar.startOfDay(for: cached.date) == today {
            return cached.profile
        }

        let computed = compute(tasks: tasks, focusBlocks: focusBlocks, now: now)
        _cache = (profile: computed, date: now)
        return computed
    }

    /// Berechnet ein frisches Profil ohne Cache (fuer Tests und erzwungene Neuberechnung).
    static func compute(
        tasks: [LocalTask],
        focusBlocks: [FocusBlock],
        now: Date = Date()
    ) -> BehavioralProfile {
        let windowStart = Calendar.current.date(byAdding: .day, value: -28, to: now) ?? now
        let windowedTasks = tasks.filter {
            $0.isCompleted,
            let completed = $0.completedAt,
            completed >= windowStart && completed <= now
        }

        return BehavioralProfile(
            computedAt: now,
            categoryTimeAffinity: computeTimeAffinity(from: windowedTasks),
            avgTasksPerDay: computeAvgTasksPerDay(from: windowedTasks),
            avgMinutesPerDay: computeAvgMinutesPerDay(from: windowedTasks, focusBlocks: focusBlocks),
            estimationFactor: computeEstimationFactor(from: windowedTasks, focusBlocks: focusBlocks)
        )
    }

    // MARK: - Invalidierung

    /// Loescht den in-memory Cache. Soll aufgerufen werden wenn neue Tasks
    /// abgeschlossen werden (z.B. in TaskCompletionService).
    static func invalidateCache() {
        _cache = nil
    }

    // MARK: - Komponente 1: Tageszeit-Affinitaet

    /// Berechnet fuer jede Kategorie den Anteil pro Tageszeit-Fenster.
    /// Nur Tasks mit gesetzter Kategorie werden gezaehlt.
    /// Gibt nil zurueck wenn weniger als minTasksForAffinity Tasks vorhanden sind.
    static func computeTimeAffinity(
        from tasks: [LocalTask]
    ) -> [TaskCategory: [DayPeriod: Double]]? {
        let categorized = tasks.filter {
            TaskCategory(rawValue: $0.taskType) != nil && $0.completedAt != nil
        }
        guard categorized.count >= minTasksForAffinity else { return nil }

        var result: [TaskCategory: [DayPeriod: Double]] = [:]

        for category in TaskCategory.allCases {
            let categoryTasks = categorized.filter { $0.taskType == category.rawValue }
            guard !categoryTasks.isEmpty else {
                // Keine Tasks in dieser Kategorie: gleichmaessige Verteilung
                result[category] = [.morning: 0.0, .afternoon: 0.0, .evening: 0.0]
                continue
            }

            var counts: [DayPeriod: Int] = [.morning: 0, .afternoon: 0, .evening: 0]
            for task in categoryTasks {
                guard let completedAt = task.completedAt else { continue }
                let period = dayPeriod(for: completedAt)
                counts[period, default: 0] += 1
            }

            let total = Double(categoryTasks.count)
            result[category] = [
                .morning:   Double(counts[.morning]   ?? 0) / total,
                .afternoon: Double(counts[.afternoon] ?? 0) / total,
                .evening:   Double(counts[.evening]   ?? 0) / total
            ]
        }

        return result
    }

    // MARK: - Komponente 2: Taegliche Kapazitaet

    /// Durchschnittliche Anzahl erledigter Tasks pro aktivem Tag.
    /// Ein aktiver Tag = mind. 1 Task an diesem Tag abgeschlossen.
    /// Gibt nil zurueck wenn weniger als minActiveDaysForCapacity aktive Tage.
    static func computeAvgTasksPerDay(from tasks: [LocalTask]) -> Double? {
        let calendar = Calendar.current
        var tasksByDay: [Date: Int] = [:]

        for task in tasks {
            guard let completedAt = task.completedAt else { continue }
            let day = calendar.startOfDay(for: completedAt)
            tasksByDay[day, default: 0] += 1
        }

        guard tasksByDay.count >= minActiveDaysForCapacity else { return nil }

        let total = tasksByDay.values.reduce(0, +)
        return Double(total) / Double(tasksByDay.count)
    }

    /// Durchschnittliche tatsaechliche Arbeitszeit pro aktivem Tag (Minuten).
    /// Basiert auf FocusBlock.taskTimes: summiert alle Sekunden fuer Tasks
    /// die an einem Tag abgeschlossen wurden.
    /// Gibt nil zurueck wenn weniger als minActiveDaysForCapacity aktive Tage.
    static func computeAvgMinutesPerDay(
        from tasks: [LocalTask],
        focusBlocks: [FocusBlock]
    ) -> Double? {
        let calendar = Calendar.current

        // Task-ID -> completedAt Tag (nur fuer Tasks im Fenster)
        var taskDayMap: [String: Date] = [:]
        for task in tasks {
            guard let completedAt = task.completedAt else { continue }
            taskDayMap[task.id] = calendar.startOfDay(for: completedAt)
        }

        // Sekunden pro Tag summieren
        var secondsByDay: [Date: Int] = [:]
        for block in focusBlocks {
            for (taskID, seconds) in block.taskTimes {
                guard let day = taskDayMap[taskID] else { continue }
                secondsByDay[day, default: 0] += seconds
            }
        }

        guard secondsByDay.count >= minActiveDaysForCapacity else { return nil }

        let totalSeconds = secondsByDay.values.reduce(0, +)
        let avgSeconds = Double(totalSeconds) / Double(secondsByDay.count)
        return avgSeconds / 60.0 // Umrechnung in Minuten
    }

    // MARK: - Komponente 3: Schaetz-Genauigkeit

    /// Faktor geschaetzte vs. tatsaechliche Dauer.
    /// Nur Tasks mit gesetztem estimatedDuration UND vorhandenen taskTimes-Eintraegen.
    /// 1.0 = perfekt, 1.5 = User braucht 50% laenger.
    /// Gibt nil zurueck wenn weniger als minTasksForEstimation verwertbare Tasks.
    static func computeEstimationFactor(
        from tasks: [LocalTask],
        focusBlocks: [FocusBlock]
    ) -> Double? {
        // Task-ID -> tatsaechliche Sekunden (Summe ueber alle Bloecke)
        var actualSecondsByTask: [String: Int] = [:]
        for block in focusBlocks {
            for (taskID, seconds) in block.taskTimes {
                actualSecondsByTask[taskID, default: 0] += seconds
            }
        }

        var ratios: [Double] = []
        for task in tasks {
            guard let estimatedMinutes = task.estimatedDuration,
                  estimatedMinutes > 0,
                  let actualSeconds = actualSecondsByTask[task.id],
                  actualSeconds > 0
            else { continue }

            let estimatedSeconds = Double(estimatedMinutes) * 60.0
            let ratio = Double(actualSeconds) / estimatedSeconds
            ratios.append(ratio)
        }

        guard ratios.count >= minTasksForEstimation else { return nil }

        return ratios.reduce(0, +) / Double(ratios.count)
    }

    // MARK: - Helpers

    /// Ordnet einen Zeitpunkt einem Tageszeit-Fenster zu.
    /// morning: 06:00-11:59, afternoon: 12:00-17:59, evening: 18:00-05:59
    static func dayPeriod(for date: Date) -> DayPeriod {
        let hour = Calendar.current.component(.hour, from: date)
        switch hour {
        case 6..<12:  return .morning
        case 12..<18: return .afternoon
        default:      return .evening
        }
    }
}
```

---

### 3. `FocusBloxTests/BehavioralProfileServiceTests.swift` (~180 LoC, CREATE)

Alle Tests pruefen Verhalten, nicht Existenz. Jeder Test muss rot werden wenn das Feature kaputt ist.

---

## Expected Behavior

- **Input:** `profile(tasks:focusBlocks:now:)` erhalt alle LocalTasks und FocusBlocks aus dem relevanten Zeitraum. Der Service filtert intern auf completed + 28-Tage-Fenster.
- **Output:** `BehavioralProfile` mit drei optionalen Metriken. Felder sind nil wenn die jeweilige Mindestschwelle unterschritten wird.
- **Side effects:**
  - Ergebnis wird in `_cache` gespeichert (in-memory, kein Disk-Write)
  - Cache wird am naechsten Kalendertag automatisch invalidiert (beim naechsten `profile()`-Aufruf)
  - `AppSettings` wird NICHT modifiziert

## API-Signaturen (Zusammenfassung)

```swift
// Haupentrypoint (mit Cache)
static func profile(tasks: [LocalTask], focusBlocks: [FocusBlock], now: Date = Date()) -> BehavioralProfile

// Direkte Berechnung ohne Cache (fuer Tests)
static func compute(tasks: [LocalTask], focusBlocks: [FocusBlock], now: Date = Date()) -> BehavioralProfile

// Cache-Invalidierung
static func invalidateCache()

// Einzelkomponenten (internal, direkt testbar)
static func computeTimeAffinity(from tasks: [LocalTask]) -> [TaskCategory: [DayPeriod: Double]]?
static func computeAvgTasksPerDay(from tasks: [LocalTask]) -> Double?
static func computeAvgMinutesPerDay(from tasks: [LocalTask], focusBlocks: [FocusBlock]) -> Double?
static func computeEstimationFactor(from tasks: [LocalTask], focusBlocks: [FocusBlock]) -> Double?
static func dayPeriod(for date: Date) -> DayPeriod
```

## Minimum-Sample-Schwellen

| Komponente | Schwelle | Default wenn unterschritten |
|-----------|----------|---------------------------|
| Tageszeit-Affinitaet | >= 10 Tasks mit gesetzter Kategorie | `categoryTimeAffinity = nil` |
| Taegliche Kapazitaet (Tasks) | >= 5 aktive Tage | `avgTasksPerDay = nil` |
| Taegliche Kapazitaet (Minuten) | >= 5 aktive Tage mit FocusBlock-Daten | `avgMinutesPerDay = nil` |
| Schaetz-Genauigkeit | >= 10 Tasks mit beiden Dauer-Werten | `estimationFactor = nil` |

## Test-Plan

### Komponente 1: Tageszeit-Affinitaet

```swift
/// Verhalten: 10 Income-Tasks alle zwischen 08:00-09:00 → income.morning = 1.0, afternoon = 0.0, evening = 0.0
/// Bricht wenn: Zeitfenster-Zuordnung falsch ist oder Anteilsberechnung fehlerhaft.
func test_timeAffinity_allMorningTasks_morningIs1()

/// Verhalten: 5 Tasks morning + 5 Tasks afternoon, Kategorie income → morning = 0.5, afternoon = 0.5
/// Bricht wenn: Anteile nicht korrekt auf 1.0 normalisiert werden.
func test_timeAffinity_evenSplit_correctProportions()

/// Verhalten: 9 Tasks (unter Schwelle) → Rueckgabe nil
/// Bricht wenn: Service trotzdem ein Ergebnis liefert statt nil.
func test_timeAffinity_belowThreshold_returnsNil()

/// Verhalten: Task um 05:59 → evening, 06:00 → morning, 11:59 → morning, 12:00 → afternoon, 17:59 → afternoon, 18:00 → evening
/// Bricht wenn: Grenzen der Zeitfenster falsch gesetzt sind (Off-by-one).
func test_dayPeriod_boundaryHours_correct()

/// Verhalten: Tasks ohne Kategorie (taskType = "") werden nicht gezaehlt, erscheinen nicht im Ergebnis
/// Bricht wenn: Uncategorized Tasks die Affinitaets-Berechnung verfaelschen.
func test_timeAffinity_uncategorizedTasksIgnored()
```

### Komponente 2: Taegliche Kapazitaet

```swift
/// Verhalten: 3 Tasks an Tag A, 5 Tasks an Tag B, 4 Tasks an Tag C, 2 Tasks an Tag D, 6 Tasks an Tag E → avgTasksPerDay = 4.0
/// Bricht wenn: Durchschnitt falsch berechnet oder inaktive Tage mitgezaehlt werden.
func test_avgTasksPerDay_fiveDays_correctAverage()

/// Verhalten: 4 aktive Tage (unter Schwelle 5) → avgTasksPerDay = nil
/// Bricht wenn: Service trotzdem einen Wert liefert.
func test_avgTasksPerDay_belowThreshold_returnsNil()

/// Verhalten: Nur Tage zaehlen, an denen mind. 1 Task abgeschlossen wurde (nicht alle Tage im Fenster)
/// Setup: 28-Tage-Fenster, aber Tasks nur an 7 Tagen → Divisor = 7, nicht 28
/// Bricht wenn: Alle Tage im Fenster als Divisor verwendet werden.
func test_avgTasksPerDay_onlyActiveDaysCount()

/// Verhalten: FocusBlock mit 3600s fuer Task A (Tag 1) + 1800s fuer Task B (Tag 1) → avgMinutes fuer Tag 1 = 90min
/// Bricht wenn: Sekunden-zu-Minuten-Umrechnung falsch ist oder Task-Tag-Zuordnung fehlerhaft.
func test_avgMinutesPerDay_correctConversion()

/// Verhalten: Tasks ohne FocusBlock-Eintrag tragen 0 Minuten bei (kein Crash, kein nil)
/// Bricht wenn: fehlende taskTimes-Eintraege zu einem Fehler fuehren.
func test_avgMinutesPerDay_taskWithoutFocusBlock_contributesZero()
```

### Komponente 3: Schaetz-Genauigkeit

```swift
/// Verhalten: 10 Tasks je 60min geschaetzt, je 90min tatsaechlich (5400s) → estimationFactor = 1.5
/// Bricht wenn: Faktor-Berechnung oder Einheiten-Umrechnung fehlerhaft.
func test_estimationFactor_50percentOver_returns1point5()

/// Verhalten: 10 Tasks je 60min geschaetzt, je 60min tatsaechlich (3600s) → estimationFactor = 1.0
/// Bricht wenn: Perfekte Schaetzung nicht als 1.0 zurueckgegeben wird.
func test_estimationFactor_perfectEstimation_returns1point0()

/// Verhalten: 9 Tasks mit beiden Werten (unter Schwelle) → estimationFactor = nil
/// Bricht wenn: Service trotzdem einen Wert liefert.
func test_estimationFactor_belowThreshold_returnsNil()

/// Verhalten: Tasks ohne estimatedDuration werden ignoriert (zaehlen nicht zur Schwelle)
/// Bricht wenn: Tasks ohne Schaetzung die Berechnung verfaelschen.
func test_estimationFactor_tasksWithoutEstimate_excluded()

/// Verhalten: Task in mehreren FocusBlocks → alle taskTimes-Eintraege werden summiert
/// Setup: Task A in Block 1 mit 1800s, in Block 2 mit 900s → tatsaechlich 2700s (45min)
/// Bricht wenn: nur der erste oder letzte Block-Eintrag verwendet wird.
func test_estimationFactor_taskInMultipleBlocks_timeSummed()
```

### Cache-Verhalten

```swift
/// Verhalten: Zweiter Aufruf mit anderem tasks-Array am selben Tag → gecachtes Ergebnis (altes Profil)
/// Bricht wenn: Cache ignoriert wird und jedes Mal neu berechnet wird.
func test_cache_sameDay_returnsCached()

/// Verhalten: Cache von gestern → heute wird neu berechnet
/// Bricht wenn: Cache ueber Tagesgrenzen hinweg nicht invalidiert wird.
func test_cache_differentDay_recomputesProfile()

/// Verhalten: invalidateCache() → naechster profile()-Aufruf berechnet neu
/// Bricht wenn: invalidateCache() den Cache nicht wirklich loescht.
func test_invalidateCache_forcesRecompute()
```

### 28-Tage-Fenster

```swift
/// Verhalten: Task von vor 29 Tagen (ausserhalb Fenster) wird nicht gezaehlt
/// Bricht wenn: Rolling Window groesser als 28 Tage ist.
func test_rollingWindow_taskOutsideWindow_excluded()

/// Verhalten: Task von vor genau 28 Tagen (inklusive) wird gezaehlt
/// Bricht wenn: Grenztag des Fensters falsch behandelt wird.
func test_rollingWindow_taskAtWindowBoundary_included()
```

## Scope-Grenzen (NICHT in diesem Ticket)

| Feature | Begruendung |
|---------|-------------|
| Kalender-Korrelation (Meeting-Dichte → Task-Completion) | Erfordert EventKit-Zugriff im Service — kommt in RW_0.2b |
| Verschiebungs-Muster (`rescheduleCount >= 3`) | Erfordert eigene Cluster-Analyse — kommt in RW_0.2b |
| Anbindung an SmartNotificationEngine (Nudge-Personalisierung) | Erfordert fertiges Profil — kommt nach RW_0.2b |
| Persistenz des Profils (SwiftData/Disk) | Nicht noetig — in-memory Cache reicht fuer Use Case |
| UI-Darstellung des Profils | Kein UI in diesem Ticket |
| `AppSettings.lastProfileComputeDate` | Nicht benoetigt — Cache-Invalidierung erfolgt rein in-memory |

## Known Limitations

- `avgMinutesPerDay` ist nil wenn Tasks zwar abgeschlossen wurden, aber keine FocusBlock-Eintraege mit `taskTimes` vorliegen — das ist korrekt, da ohne gemessene Zeit keine valide Kapazitaets-Aussage moeglich ist
- Tasks die in mehreren FocusBlocks bearbeitet wurden werden korrekt summiert, aber der Task zaehlt nur einmal zum Tages-Divisor (da `completedAt` eindeutig ist)
- `DayPeriod.evening` umfasst 18:00-05:59 (10 Stunden) gegenueber morning/afternoon mit je 6 Stunden — bewusste Entscheidung, da Abend-Produktivitaet selten und semantisch eigenstaendig ist
- macOS: Shared Service, kein plattformspezifischer Code noetig

## Changelog

- 2026-03-23: Initial spec created (Phase 1 — Core Statistics, 3 Komponenten)
  - Scope: 2 neue Dateien Production + 1 Test-Datei
  - Nicht in Scope: Kalender-Korrelation, Verschiebungs-Muster (→ RW_0.2b)
